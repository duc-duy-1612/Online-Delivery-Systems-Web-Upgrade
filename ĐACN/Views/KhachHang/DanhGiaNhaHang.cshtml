@model ĐACN.NhaHang
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Đánh giá nhà hàng";
    var nhaHang = ViewBag.NhaHang as ĐACN.NhaHang;
}

<style>
    body {
        background-color: #f8f9fa;
    }
    .review-container {
        max-width: 650px;
        margin: 40px auto;
        position: relative;
    }
    .card-review {
        border: none;
        box-shadow: 0 10px 40px rgba(0,0,0,0.08);
        border-radius: 1.25rem;
        background: #ffffff;
        overflow: hidden;
    }
    .card-review-header {
        background: linear-gradient(135deg, #0d6efd 0%, #0dcaf0 100%);
        color: white;
        padding: 2rem 1.5rem;
        text-align: center;
        border-radius: 1.25rem 1.25rem 0 0;
    }
    .avatar-lg {
        width: 100px;
        height: 100px;
        border-radius: 50%;
        object-fit: cover;
        border: 4px solid #fff;
        box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        margin-top: -50px;
        background: #fff;
    }
    .rating-star {
        font-size: 2.5rem;
        color: #e2e8f0;
        cursor: pointer;
        transition: all 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        display: inline-block;
        margin: 0 4px;
    }
    .rating-star:hover {
        transform: scale(1.2);
    }
    .rating-star.selected {
        color: #ffc107;
        filter: drop-shadow(0 2px 4px rgba(255, 193, 7, 0.3));
    }
    .rating-star.selected.pop {
        animation: popStar 0.4s ease-out;
    }
    @@keyframes popStar {
        0% { transform: scale(1); }
        50% { transform: scale(1.3); }
        100% { transform: scale(1); }
    }
    .form-control.review-textarea {
        border-radius: 0.75rem;
        border: 2px solid #e2e8f0;
        padding: 1rem;
        background-color: #f8f9fa;
        transition: all 0.3s ease;
    }
    .form-control.review-textarea:focus {
        background-color: #fff;
        border-color: #0d6efd;
        box-shadow: 0 0 0 4px rgba(13, 110, 253, 0.1);
    }
    .btn-action {
        border-radius: 0.75rem;
        font-weight: 600;
        padding: 0.75rem 1.5rem;
        transition: all 0.3s ease;
    }
    .btn-action:hover:not(:disabled) {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(25, 135, 84, 0.3);
    }
    .btn-back {
        border-radius: 0.75rem;
        font-weight: 600;
        padding: 0.75rem 1.5rem;
        transition: all 0.3s ease;
        border: 2px solid #e2e8f0;
        color: #6c757d;
        text-decoration: none;
    }
    .btn-back:hover {
        background-color: #f8f9fa;
        color: #495057;
    }
</style>

<div class="container mb-5">
    <div class="review-container">
        
        @if (TempData["Msg"] != null)
        {
            <div class="alert alert-info alert-dismissible fade show shadow-sm border-0 mb-4 rounded-3" role="alert">
                <i class="fas fa-info-circle me-2 text-primary"></i> @TempData["Msg"]
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        }

        <div class="card card-review">
            <div class="card-review-header">
                <h3 class="mb-1 fw-bold">Đánh giá nhà hàng</h3>
                <p class="mb-0 opacity-75">Cảm ơn bạn đã lựa chọn món ăn tại đây</p>
            </div>
            
            <div class="card-body text-center pt-0 px-4 pb-4">
                <img src="~/Content/Images/restaurant-icon.png" alt="Restaurant" class="avatar-lg mb-3" onerror="this.src='https://ui-avatars.com/api/?name=NH&background=0D8ABC&color=fff&size=100'" />
                <h4 class="fw-bold text-dark mb-1">@nhaHang.TenNH</h4>
                <p class="text-muted small mb-4"><i class="fas fa-map-marker-alt text-danger me-1"></i> @nhaHang.DiaChi</p>

                @using (Html.BeginForm("LuuDanhGiaNhaHang", "KhachHang", FormMethod.Post, new { id = "formDanhGia" }))
                {
                    @Html.Hidden("maNH", nhaHang.MaNH)
                    <input type="hidden" name="soSao" id="soSao" value="0" />

                    <div class="mb-4">
                        <div class="d-flex justify-content-center" id="star-container">
                            <i class="fas fa-star rating-star" data-value="1"></i>
                            <i class="fas fa-star rating-star" data-value="2"></i>
                            <i class="fas fa-star rating-star" data-value="3"></i>
                            <i class="fas fa-star rating-star" data-value="4"></i>
                            <i class="fas fa-star rating-star" data-value="5"></i>
                        </div>
                        <div class="mt-3 fs-5 fw-bold" id="rating-text" style="color: #cbd5e0; min-height: 28px;">Chưa chọn</div>
                        <div class="small text-danger mt-1" id="ratingError" style="display: none;">Vui lòng chọn số sao đánh giá</div>
                    </div>

                    <div class="mb-4 text-start">
                        <label class="fw-bold mb-2 text-secondary">Chi tiết đánh giá (Tùy chọn)</label>
                        <textarea class="form-control review-textarea" name="binhLuan" id="binhLuan" rows="4" placeholder="Chia sẻ trải nghiệm của bạn về nhà hàng này..."></textarea>
                    </div>

                    <div class="d-flex justify-content-between gap-3">
                        <a href="@Url.Action("XemMenu", "KhachHang", new { id = nhaHang.MaNH })" class="btn btn-back flex-grow-1">
                            <i class="fas fa-arrow-left me-2"></i> Quay lại
                        </a>
                        <button type="submit" class="btn btn-success btn-action flex-grow-1" id="btnSubmit" disabled>
                            <i class="fas fa-paper-plane me-2"></i> Gửi đánh giá
                        </button>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<script>
    const ratingDescriptions = {
        1: "Rất tệ",
        2: "Không hài lòng",
        3: "Bình thường",
        4: "Hài lòng",
        5: "Tuyệt vời"
    };

    document.addEventListener("DOMContentLoaded", function() {
        const stars = document.querySelectorAll('#star-container .rating-star');
        const input = document.getElementById('soSao');
        const textLabel = document.getElementById('rating-text');
        const btnSubmit = document.getElementById('btnSubmit');
        const ratingError = document.getElementById('ratingError');

        stars.forEach(star => {
            star.addEventListener('click', function () {
                const value = this.getAttribute('data-value');
                input.value = value;
                
                ratingError.style.display = 'none';
                btnSubmit.disabled = false;

                stars.forEach(s => {
                    s.classList.remove('pop');
                    if (s.getAttribute('data-value') <= value) {
                        s.classList.add('selected');
                    } else {
                        s.classList.remove('selected');
                    }
                });
                
                // Add pop animation to the clicked star
                this.classList.add('pop');
                setTimeout(() => this.classList.remove('pop'), 400);

                textLabel.textContent = ratingDescriptions[value];
                
                if(value <= 2) {
                    textLabel.className = "mt-3 fs-5 fw-bold text-danger";
                } else if(value == 3) {
                    textLabel.className = "mt-3 fs-5 fw-bold text-info";
                } else {
                    textLabel.className = "mt-3 fs-5 fw-bold text-warning";
                }
            });
            
            // Hover effects
            star.addEventListener('mouseenter', function() {
                const value = this.getAttribute('data-value');
                stars.forEach(s => {
                    if (s.getAttribute('data-value') <= value) {
                        s.style.opacity = '1';
                        s.style.transform = 'scale(1.1)';
                    } else {
                        s.style.opacity = '0.5';
                    }
                });
            });
            
            star.addEventListener('mouseleave', function() {
                stars.forEach(s => {
                    s.style.opacity = '1';
                    s.style.transform = 'scale(1)';
                });
            });
        });
        
        document.getElementById('formDanhGia').addEventListener('submit', function(e) {
            if (parseInt(input.value) === 0) {
                e.preventDefault();
                ratingError.style.display = 'block';
            }
        });
    });
</script>