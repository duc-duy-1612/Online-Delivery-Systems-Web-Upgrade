@model IEnumerable<ĐACN.NhaHang>

@{
    ViewBag.Title = "Danh sách cửa hàng";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h2 class="fw-bold mb-1" style="color: #111827;">Danh sách cửa hàng đối tác</h2>
        <p class="text-muted mb-0">Quản lý và tra cứu thông tin các nhà hàng trên hệ thống.</p>
    </div>
    <a href="@Url.Action("ThemNhaHang", "Admin")" class="btn btn-dark btn-modern">
        Thêm nhà hàng mới
    </a>
</div>

<div class="admin-card border-0 mb-4 p-3">
    <form id="filterForm" method="get" class="row g-3 align-items-center">
        <div class="col-md-5">
            <input type="text" id="searchInput" name="keyword" value="@ViewBag.Keyword" placeholder="Nhập tên hoặc địa chỉ cửa hàng..." class="form-control bg-light border-0 shadow-none" style="padding: 10px 16px; border-radius: 8px;" autocomplete="off" />
        </div>
        <div class="col-md-4">
            <select id="statusSelect" name="status" class="form-select bg-light border-0 shadow-none" style="padding: 10px 16px; border-radius: 8px;">
                <option value="">Tất cả trạng thái</option>
                <option value="Hoạt động" @(ViewBag.Status == "Hoạt động" ? "selected" : "")>Đang hoạt động</option>
                <option value="Bị khóa" @(ViewBag.Status == "Bị khóa" ? "selected" : "")>Đã khóa</option>
            </select>
        </div>
        <div class="col-md-3">
            <button class="btn btn-outline-dark btn-modern w-100" style="padding: 10px 16px;">Tìm kiếm</button>
        </div>
    </form>
</div>

@if (TempData["Message"] != null)
{
    <div class="alert alert-success border-0 shadow-sm mb-4" style="border-radius: 8px; background-color: #d1fae5; color: #065f46;">
        @TempData["Message"]
    </div>
}

<div class="admin-card border-0 p-0 overflow-hidden">
    <div class="table-responsive">
        <table class="table align-middle mb-0">
            <thead style="background-color: #f9fafb;">
                <tr>
                    <th class="text-muted fw-bold border-bottom-0 py-3 ps-4" style="font-size: 0.75rem; text-transform: uppercase; letter-spacing: 1px;">STT</th>
                    <th class="text-muted fw-bold border-bottom-0 py-3" style="font-size: 0.75rem; text-transform: uppercase; letter-spacing: 1px;">Nhà hàng</th>
                    <th class="text-muted fw-bold border-bottom-0 py-3" style="font-size: 0.75rem; text-transform: uppercase; letter-spacing: 1px;">Liên hệ</th>
                    <th class="text-muted fw-bold border-bottom-0 py-3" style="font-size: 0.75rem; text-transform: uppercase; letter-spacing: 1px;">Tài khoản</th>
                    <th class="text-muted fw-bold border-bottom-0 py-3 text-center" style="font-size: 0.75rem; text-transform: uppercase; letter-spacing: 1px;">Trạng thái</th>
                    <th class="text-muted fw-bold border-bottom-0 py-3 pe-4 text-end" style="font-size: 0.75rem; text-transform: uppercase; letter-spacing: 1px;">Thao tác</th>
                </tr>
            </thead>
            <tbody id="restaurantTableBody">
                @{ int stt = 1; }
                @foreach (var nh in Model)
                {
                    <tr class="border-bottom" style="transition: all 0.2s ease;">
                        <td class="py-3 ps-4 text-muted">@stt</td>
                        <td class="py-3">
                            <div class="fw-bold nh-name" style="color: #111827;">@nh.TenNH</div>
                            <div class="text-muted small text-truncate nh-address" style="max-width: 200px;">@nh.DiaChi</div>
                        </td>
                        <td class="py-3 text-dark fw-medium">@nh.SDT</td>
                        <td class="py-3">
                            <div class="text-dark">@(nh.TaiKhoan?.TenDangNhap ?? "Chưa có")</div>
                            <div class="text-muted small">@(nh.TaiKhoan?.VaiTro ?? "-")</div>
                        </td>
                        <td class="py-3 text-center">
                            @if (nh.TaiKhoan?.TrangThai == true)
                            {
                                <span class="badge bg-success bg-opacity-10 text-success fw-normal px-3 py-2 nh-status" style="border-radius: 50px;">Hoạt động</span>
                            }
                            else
                            {
                                <span class="badge bg-danger bg-opacity-10 text-danger fw-normal px-3 py-2 nh-status" style="border-radius: 50px;">Bị khóa</span>
                            }
                        </td>
                        <td class="py-3 pe-4 text-end">
                            <a href="@Url.Action("ChiTietNhaHang", "Admin", new { id = nh.MaNH })" class="btn btn-sm btn-light fw-medium px-3" style="border-radius: 6px;">
                                Chi tiết
                            </a>
                        </td>
                    </tr>
                    stt++;
                }
            </tbody>
        </table>
    </div>
</div>

@section scripts {
    <script>
        // Hàm loại bỏ dấu tiếng Việt để tìm kiếm chính xác
        function removeAccents(str) {
            if (!str) return '';
            return str.normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase();
        }

        $(document).ready(function () {
            // Ngăn form submit reload trang
            $('#filterForm').on('submit', function(e) {
                e.preventDefault();
            });

            // Gắn sự kiện khi gõ phím hoặc đổi trạng thái
            $('#searchInput, #statusSelect').on('input change', function () {
                var keyword = removeAccents($('#searchInput').val());
                var statusFilter = $('#statusSelect').val(); // "Hoạt động", "Bị khóa", hoặc ""

                $('#restaurantTableBody tr').each(function () {
                    var row = $(this);
                    var name = removeAccents(row.find('.nh-name').text());
                    var address = removeAccents(row.find('.nh-address').text());
                    var status = row.find('.nh-status').text().trim();

                    // Kiểm tra xem dòng này có khớp với từ khóa tìm kiếm không (tên hoặc địa chỉ)
                    var matchKeyword = keyword === '' || name.includes(keyword) || address.includes(keyword);
                    
                    // Kiểm tra xem dòng này có khớp với trạng thái không
                    var matchStatus = statusFilter === '' || status === statusFilter;

                    if (matchKeyword && matchStatus) {
                        row.show();
                    } else {
                        row.hide();
                    }
                });
            });
        });
    </script>
}